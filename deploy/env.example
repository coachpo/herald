# Spear configuration (compose interpolation + runtime env).
# Copy to `spear/.env` and fill values.

# Prebuilt image tags (or keep defaults to use local images).
BACKEND_IMAGE=ghcr.io/OWNER/REPO-backend:latest
FRONTEND_IMAGE=ghcr.io/OWNER/REPO-frontend:latest

# Postgres credentials used by `spear/docker-compose.spear.yml`.
POSTGRES_DB=beacon
POSTGRES_USER=beacon
POSTGRES_PASSWORD=change-me

# Backend (Django) runtime configuration.
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SECRET_KEY=change-me

# Used in verification email links.
NEXT_PUBLIC_BASE_URL=http://localhost:3100

# Auth/token settings.
JWT_SIGNING_KEY=change-me
TOKEN_HASH_KEY=

# Feature flags.
ALLOW_USER_SIGNUP=true

# Channel config encryption.
CHANNEL_CONFIG_ENCRYPTION_KEY=

# Optional mail settings.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
DEFAULT_FROM_EMAIL=no-reply@localhost

# Frontend (Next.js) runtime configuration.
# Internal base URL for Next.js to proxy /api/* to the backend container.
BACKEND_BASE_URL=http://backend:8100
